# Quant Cloud: run on ARM64 to match the arm64 images the org builds.
x-quant-labels:
  quant.architecture: ARM64

services:
  app:
    build:
      context: .
      args:
        NODE_VERSION: "22"
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
